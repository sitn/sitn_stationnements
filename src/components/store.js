import { reactive } from 'vue'

export const store = reactive({
  count: 0
})


class Mob20 {
    constructor(type, area) {
      this.type = type;
      this.area = area;
    }
  }

let locations = []
locations.push()